# wqs二分补充

~~PS:很多人认为oiwiki写得非常差，其实是其写法与oi选手的需求和能力重合性不好。其中大量的数学公式劝退了许多尝试阅读的选手。但oiwiki这种写法更具有专业性，也结合了许多赛题中的特殊情况，建议大家可以尝试阅读原文。~~

## wqs二分基础

这是最基础的wqs二分理解，如果理解了这部分，你可以说你学会wqs二分了，能写模板题，然后仅此而已。

### wqs二分条件

比如，假设要解决的问题是，要从$n$ 个物品中选取$m$ 个，并最优化某个较复杂的目标函数．如果设从前 𝑖$i$ 个物品中选取$j$ 个，目标函数的最优值为 $f(i,j)$，那么原问题的答案就是$f(n,m)$。

假设$f(n,m)$函数关于$m$是凸的（也就是随$m$增长，$f(n,m)$的增长（降低）越来越慢），那么就可以wqs二分。

### wqs二分思路

进一步假设，没有数量限制的最优化问题容易解决．但是，选取到的最优数量未必满足原问题的数量限制．假设选取的物品过多．那么，就可以考虑在选取物品时，为每个选取到的物品都附加一个固定大小的惩罚 $k$（即「带权二分」中的「权」），仍然解没有数量限制的最优化问题．根据 $k$ 的取值不同，选取到的最优数量也会有所不同；而且，随着 $k$ 的变化，选取到的最优数量也是单调变化的。

也就是说记带惩罚的问题的目标函数的最优值为$f'(i,j)$，此时要求的是$min\{f'(n,i)\}$，也就是说你的$k$增长，最终$\{f'(n,i)\}$中最小的$i$的变化也是单调的。即惩罚越大，取到最小值的$i$越小。

e.g. 假如你水社区会很开心，尤其是在你很久没看社区后，都是新鲜的东西，你会更开心。但是水社区越多，你水社区的开心程度就会降低（水社区的收益是过于次数的凸函数）。

你总希望你的开心程度总和最大（无惩罚问题，简单dp）。

你的教练希望你一周**恰好**水$k$次社区，保证你身心健康的同时，能高效地学习。但是他记不住所有人上次什么时候水社区（代价过大）。

于是对于所有水社区的人他都批评一下，对大家额外造成一个负的开心程度（惩罚）。

如果他惩罚力度过大，会导致大家根本不会去水社区。如果惩罚过小，则大家水社区的次数超过限制。在二分惩罚，得到一个合适的惩罚力度后，这样就能计算出大家每周只**恰好**水$k$次社区能获得的**最大开心程度**（需要把惩罚的数值加回去）。

### wqs二分做法

初始时，选取一个 $\lambda$ 的合理的区间；
在当前的区间中选择一个 $\lambda$；
求解带惩罚的问题 $h(\lambda)=\min_{x\in X}f(x)-\lambda g(x)$，并记录它的最优解 $x_\lambda$ 处 $g(x)$ 的取值 $g(x_\lambda)$；
如果 $g(x_\lambda) = y$，就得到原问题的最优价值 $v(y) = h(\lambda) + \lambda y$，直接结束算法；
否则，根据 $g(x_\lambda)$ 与 $y$ 的大小关系，调整 $\lambda$ 的区间，并回到步骤 2．

```c++
// 核心算法：二分搜索λ求解 min f(x) s.t. g(x)=y
// solve(λ): 返回 min f(x)-λg(x) 的最优解对应的g(x)值

double l = L0, r = R0;  // λ的初始区间
while (r - l > eps) {
    double mid = (l + r) / 2;
    double gx = solve(mid);  // 求解子问题并返回g(xλ)
    
    if (fabs(gx - y) < eps) {
        // 找到精确解，可提前退出
        break;
    }
    if (gx < y) r = mid;  // g(xλ) < y，减小λ
    else l = mid;         // g(xλ) > y，增大λ
}

// 最终结果
double lambda = (l + r) / 2;
solve(lambda);  // 再求解一次得到xλ
double ans = f(x) + lambda * (y - g(x));  // 或直接使用 h(λ) + λy
```

## 为什么wqs二分错了？

大家经常把wqs写挂，避免总是出现在~~进食后人~~，大家记住这几点。

### 原因1：f(n,m)根本不是关于m的凸函数

不是，这就根本不是wqs二分题目。

那如何证明函数的**凸性**，我们下个章节解释。

### 原因2：有平台！

下个章节我们介绍，如果出现一些$f(n,m)$三点共线的情况可能会使二分直接无法正常运行。

此处建议大家二分的代价一定选择浮点数即可避免，但浮点数也可能导致精度问题，需要大家酌情考虑使用。

### 原因3：范围爆炸！

$\lambda * g(X)$ 在二分中很可能突破int甚至是long long范围，需要考虑这种情况。

## wqs二分深入

我们最优值dp本质其实是找到一个最好的策略，我们可以把每种策略用一些一些方式表示出来，记为$x$，然后把所有策略构成的集合记录为$X$，这样我们原来的问题就可以写成：

$$f(n,m) = min\{f(x)|g(x) == m\}$$

其中$f(x)$表示策略$x$的目标函数取值，$g(x)$表示策略$x$取的物品数量。

### 几何理解

考察集合

$$
D=\{(g(x), f(x)) \in \mathbb{R} \times \mathbb{R}^d : x \in X\}
$$

如图：

![image](image\wqs二分\wqs-f-g-space.svg)

图中的红点和蓝点是 $X$ 中所有可能的选择投影在平面 $(g(x), f(x))$ 上得到的集合 $\mathcal D$．于是，原问题所要求的就是横坐标为 $y$ 的那些点中，纵坐标的最小值 $v(y)$．当 $y$ 变动时，所有这样的点 $(y,v(y))$ 就构成了图中的红点的集合．

为了求得点 $(y,v(y))$ 的纵坐标，可以考虑用斜率为 $\lambda \in \mathbf R$ 的直线去切集合 $\mathcal D$．如图所示，当直线的斜率选取得恰当时，经过点 $(y,v(y))$ 的那条直线，是所有经过集合 $\mathcal D$ 中的点且斜率为 $\lambda$ 的直线中，截距 $f(x) - \lambda g(x)$ 最小的．将这一最小值记为

$$
h(\lambda) = \min_{x \in X} f(x) - \lambda g(x).
$$

那么，因为 $(y,v(y))$ 同样位于该直线上，就可以得到原问题的解

$$
v(y) = h(\lambda) + \lambda y.
$$

### 需要对偶方法证明解决的复杂问题（很难考到，感兴趣可以阅读oiwiki）

刚才的问题我们主要考察了限制只有一个维度情况，也就是形式如$f(n,m)$的问题，如果限制再多一些维度：

比如，假设要解决的问题是，要从$n$个物品中选取$m1$个放入盒子1，选取$m2$个放入盒子2，并最优化某个较复杂的目标函数。其也可以通过一样的wqs二分做法解决。

oiwiki中有一节就是介绍这个，但是这个距离大家有点遥远了，感兴趣的可以阅读，培养自己的数学理解能力，推荐初中的同学（有追求的），去oiwiki阅读该部分。

### 凸性证明

也就是证明：

上文f(n,m)是关于m的凸函数

可以解决地问题可以如下转化：

#### 归约为含参凸优化

$$
v(y) = \inf_{x \in \mathcal D(y)} f(x,y).
$$

其中，目标函数 $f:\mathbf R^n \times \mathbf R^d \rightarrow \mathbf R \cup \{\pm\infty\}$ 对于每个 $y \in \mathbf R^d$ 都是关于 $x \in \mathbf R^m$ 的凸函数，而可行域 $\mathcal D:\mathbf R^d \rightarrow \mathcal P(\mathbf R^m)$ 是 $\mathbf R^d$ 上的集合值函数，且对于每个 $y \in \mathbf R^d$，集合 $\mathcal D(y)$ 都是凸集．这些条件保证了对于任意参数 $y \in \mathbf R^d$，这都是一个凸优化问题．

上面是oiwiki的原文，完全看不懂？因为oiwiki喜欢将问题推广到oi比赛以外的难度：

$\inf$ 是下确界的意思，我们在oi比赛中可以认为是$min$

$f:\mathbf R^n \times \mathbf R^d \rightarrow \mathbf R \cup \{\pm\infty\}$ 表示$f$函数的参数是两个向量，值是实数或无穷。但我们的oi比赛中怎么会有无穷，我们处理的问题很多时候也是一维度的。

凸集是指在集合内任意两点之间的连线段也完全包含在该集合内的点集。也就是说我们认为策略要是**连续**的。

**如果你愿意查资料，多阅读。把这个读懂了，那你以后这样学大多数理工科都会成功的。**

但如果读不懂，你就需要向别人**请教**了。（线下问或洛谷私信，邮箱，vjudge看到我都会回。）

**而绝对不是放弃！**~~你现在放弃搞明白它的机会，就是做出一个不优的决策，就是在浪费你的人生。~~

利用图论模型为实际问题建模时，通常有隐含的整数限制，例如一条边只能选或不选、流量只能是整数等．因此，它们只能转化为整数线性规划（integer linear programming, ILP）问题而非线性规划（LP）问题．因为 ILP 问题并非凸优化问题，所以它的价值函数未必是该问题的参数的凸函数．将一个 ILP 问题中的整数约束松弛掉后就得到一个 LP 问题，但后者未必存在满足整数约束的最优解．因此，松弛整数约束后得到的 LP 的最优价值有可能严格优于相应的 ILP 问题，两者未必等价。

而我们比赛中整数问题很常见，靠这个依据很难判断是否符合wqs二分条件。

P.S. 常见的网络流问题（最小费用最大流），是线性规划（LP）问题，也是关于流量的凸函数问题。

检验是否可以wqs二分可以看是否可以完成在**给定限制条件维度已经限定时**的网络流问题建图。

#### 归约为含参凸优化

尽管状态转移方程无法提供有效的计算方式，但是，它常常可以用于证明状态函数 $f(i,j)$ 对于参数 $j$ 具有凸性．具体地说，就是将 $f(i, \cdot)$ 这一函数视为 $i$ 处的状态，就可以将关于 $f(i,j)$ 的状态转移方程看作是关于 $f(i, \cdot)$ 的递推关系，从而可以归纳地证明每个 $f(i, \cdot)$ 都是凸函数．这类证明凸性的方法在 Slope Trick 优化 DP 的场景中更为常见，该页面也讨论了常见的保持凸性的变换．

也就是说常常可以利用类似于**数学归纳法**的方法证明。

#### 四边形不等式

算法竞赛中，另一类常见的成立凸性的问题是**区间分拆问题**．该页面证明了，如果单个区间的成本函数满足四边形不等式，那么限制区间个数的区间分拆问题的最小成本是区间个数的凸函数．该页面同样提供了一些判断某个函数 $w(l,r)$ 是否满足四边形不等式的方法．最为直接的方法就是计算它的二阶混合差分：

$$
\begin{aligned}
\Delta_l \Delta_r w(l,r) &= \Delta_l(w(l,r+1)-w(l,r)) \\
&= w(l+1,r+1)-w(l+1,r)-w(l,r+1)+w(l,r).
\end{aligned}
$$

函数 $w(l,r)$ 满足四边形不等式，当且仅当 $\Delta_l \Delta_r w(l,r)$ 非正．直观上，满足四边形不等式的函数通常意味着，区间向两侧扩大——即左端点向左移动和右端点向右移动——具有某种协同效应

#### 交换论证

组合优化问题中，证明价值函数的凸性常常会用到**交换论证**（exchange argument）．具体地说，就是从参数为 $m-1$ 和 $m+1$ 的问题的最优解出发，通过交换部分元素，构造出参数为 $m$ 且价值不超过 $(v(m-1)+v(m+1))/2$ 的可行解，从而利用 $v(m)$ 的最优性来证明凸性的论证方法．相较于凸优化的情形，组合优化问题中并不存在自然地构造两个解的「中间形态」的方法，因此，交换论证的应用通常具有一定的技巧性．

## 练习

### most important

[problem:洛谷-P1792](https://www.luogu.com.cn/problem/P1792)

完成该题，再次阅读oiwiki中的证明部分，其包含每一个证明方法对此题凸函数性质的证明。

### good problem

[problem:洛谷-P4383](https://www.luogu.com.cn/problem/P4383)

wqs二分不止能优化一个维度，甚至可以把NP做法变成P做法

[problem:洛谷-P5308](https://www.luogu.com.cn/problem/P5308)

顺便练习斜率优化

### challenge

思考两个练习题如何证明目标函数凸性

